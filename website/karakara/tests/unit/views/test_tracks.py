import pytest

import logging
log = logging.getLogger(__name__)


import karakara.views.tracks

class TestViewTracks():

    def test_track(self, tracks):
        #track = karakara.views.tracks.track_view(gen_request(id='t1'))
        #print(track)
        #assert False
        pass
        
