import pytest

#from karakara.model.model_tracks import Track


@pytest.mark.parametrize(('tags'), [
    ([]),
])
def test_track_status(tags):
    pass
    #Track._status(tags)


def test_track_status_track():
    #tracks, DBSession
    #for track in tracks:
    #    DBSession.add(track)
    #    track.status
    pass
