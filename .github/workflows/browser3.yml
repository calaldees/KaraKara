name: Browser3

on:
  push:
    paths: &paths
      - "browser3/**"
      - ".github/workflows/browser3.yml"
  pull_request:
    paths: *paths

jobs:
  publish:
    name: Docker Build
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Test
        run: docker build --target test .
        working-directory: browser3
      - name: Build
        run: docker build --target production .
        working-directory: browser3
