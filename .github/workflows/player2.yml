name: Player2

on:
  push:
    paths:
    - 'player2/**'
    - '.github/workflows/player2.yml'

jobs:
  publish:
    name: Docker Build
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master
    - name: Build
      run: docker build .
      working-directory: player2
