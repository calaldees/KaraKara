input {
#stdin { } 
#file {
#path=>'test.log'
#codec=>json
#}
}

filter {

}

output {
	#elasticsearch_http {
	#	host => localhost
	#	index => "karakara-%{event}"
	#}
	stdout {
		codec => rubydebug
	}
}
