input {
#stdin { } 
#file {
#path=>'test.log'
#codec=>json
#}
}

filter {
	date {
		match => [ "timestamp" , "ISO8601" ]
		remove_field => [ "timestamp" ]
	}
}

output {
	elasticsearch_http {
		host => localhost
		index => "karakara"
		#index => "karakara-%{event}"
	}
	#stdout {
	#	codec => rubydebug
	#}
}
