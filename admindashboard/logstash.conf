input {
#stdin { } 
#file {
#path=>'test.log'
#codec=>json
#}
}

filter {
	date {
		match => [ "timestamp" , "ISO8601" ]
	}
}

output {
	#elasticsearch_http {
	#	host => localhost
	#	index => "karakara-%{event}"
	#}
	stdout {
		codec => rubydebug
	}
}
